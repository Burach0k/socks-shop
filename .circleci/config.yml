jobs:
  deploy:
    executor: heroku/default
    steps:
      - checkout
      - heroku/install
      - common/npm-install
      - run:
          path: ./static
          command: |
            "ls"
            "sudo apt-get install -y nodejs && npm install && npm run build"
            "echo -------------"
      - heroku/deploy-via-git
orbs:
  heroku: circleci/heroku@1.2.4
version: 2.1
workflows:
  heroku_deploy:
    jobs:
      - deploy:
          context: test
